#!/bin/bash

set -eufo pipefail
echo "03-configure.sh"

# change shell
echo "sudo chsh -s $(which zsh) $USER"

{{ if .host.headless }}
# server configure

# docker
sudo usermod -aG docker $USER # need to logout & login again
{{ else }}
# desktop configure
{{ end }}
