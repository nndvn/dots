packages:
  brave-browser:
    cask: brave-browser
    snap: brave
  warp-terminal:
    apt: https://app.warp.dev/get_warp?package=deb
    cask: warp
  zed-editor:
    cask: zed
    script: curl -f https://zed.dev/install.sh | sh
